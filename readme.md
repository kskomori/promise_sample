#
```
mkdir src
mkdir dist
touch .gitignore
git init
npm init
```

```
npm install --save-dev webpack webpack-cli
npm install --save-dev eslint
npm install --save-dev @babel/cli @babel/core @babel/preset-env babel-loader @babel/polyfill

npm install --save node-fetch whatwg-fetch dom4 url-search-params
```
